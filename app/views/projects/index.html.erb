<h2 id="page_title">Stuff I 've built</h2>

<div id="posts_wrapper" class="skinny_wrapper">
  <% @projects.each do |project| %>
    <div class="post">
      <p class="date"><%= project.created_at.strftime("%A, %B %d") %></p>
      <h5><%= link_to project.title, project %></a></h5>
      <hr>
    </div>
  <% end %>
</div>
